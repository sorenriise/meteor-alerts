template(name="alert")
	if $.Session.get 'Alert'
		with $.Session.get 'Alert'
			#meteor-alert.alert(class="#{bsClass}")
				button.close(type='button')
					span(aria-hidden='true') ×
				h4 #{title}
				p
					!{html}